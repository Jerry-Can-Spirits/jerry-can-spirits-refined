---
// src/components/BrandStory.astro
---

<section class="brand-story" id="brand-story">
  <div class="story-container">
    <div class="image-column">
      <picture>
        <source 
          media="(max-width: 768px)" 
          srcset="/brand-story-image-mobile.jpg"
        >
        <source 
          media="(min-width: 769px)" 
          srcset="/brand-story-image.jpg"
        >
        <img 
          src="/brand-story-image.jpg" 
          alt="Jerry Can Spirits being enjoyed on an outdoor adventure" 
          loading="lazy"
          width="500"
          height="600"
          class="story-image"
        />
      </picture>
      <div class="image-accent"></div>
    </div>
    
    <div class="text-column">
      <div class="content-wrapper">
        <span class="section-label">Our Story</span>
        <h2 class="title">
          <span class="title-line">FROM CLEAR SIGNALS.</span>
          <span class="title-line">TO EXCEPTIONAL SPIRITS.</span>
        </h2>
        
        <div class="story-text">
          <p>
            Our story began not in a distillery, but in the Royal Corps of Signals. As a signals soldier, the mission was absolute precision, every time. That ethos, 'Swift and Sure', is the foundation of our craft—a relentless attention to detail forged through experience.
          </p>
          <p>
            Today, we apply that same discipline to the art of distillation. We've engineered an exceptionally smooth rum that honours a legacy of resilience and is built for the modern-day explorer. This is the new standard for your next adventure.
          </p>
        </div>
        
        <a href="/ethos" class="story-cta">
          <span>Discover Our Ethos</span>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>

<script>
  // Intersection Observer for scroll animations
  document.addEventListener('DOMContentLoaded', () => {
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };
    
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    }, observerOptions);
    
    // Observe elements
    const brandStory = document.querySelector('.brand-story');
    if (brandStory) {
      observer.observe(brandStory);
    }
  });
</script>